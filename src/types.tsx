export interface IDepartment {
  _id: string;
  name: string,
  description?: string,
}

export interface IEmployee {
  _id: string;
  email: string,
  name: string,
  age: number,
  position: string,
}
